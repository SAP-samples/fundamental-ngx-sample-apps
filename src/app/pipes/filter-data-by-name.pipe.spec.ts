import { FilterDataByNamePipe } from './filter-data-by-name.pipe';

describe('FilterDataByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDataByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
